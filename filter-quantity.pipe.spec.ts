import { FilterQuantityPipe } from './filter-quantity.pipe';

describe('FilterQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});

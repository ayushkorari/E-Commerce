import { ProductRoutePipe } from './product-route.pipe';

describe('ProductRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
